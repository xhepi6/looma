services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "${API_PORT:-8000}:8001"
    environment:
      - APP_ENV=dev
      - APP_SECRET_KEY=${APP_SECRET_KEY:-dev-secret-key}
      - DATABASE_URL=sqlite+aiosqlite:////data/app.db
      - CORS_ORIGINS=http://localhost:${WEB_PORT:-5173},http://localhost:${API_PORT:-8000},http://127.0.0.1:${WEB_PORT:-5173}
      - SEED_ADMIN_USERS=true
      - SEED_USER_1_USERNAME=${SEED_USER_1_USERNAME:-alice}
      - SEED_USER_1_PASSWORD=${SEED_USER_1_PASSWORD:-password123}
      - SEED_USER_2_USERNAME=${SEED_USER_2_USERNAME:-bob}
      - SEED_USER_2_PASSWORD=${SEED_USER_2_PASSWORD:-password123}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TRANSLATION_MODEL=${TRANSLATION_MODEL:-google/gemini-2.0-flash-001}
      - TRANSLATION_ENABLED=${TRANSLATION_ENABLED:-true}
      - TMDB_API_KEY=${TMDB_API_KEY:-}
      - TMDB_ENABLED=${TMDB_ENABLED:-true}
    volumes:
      - sqlite_data:/data
    networks:
      - looma-local
    restart: unless-stopped

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile.local
    ports:
      - "${WEB_PORT:-5173}:5173"
    environment:
      - API_PROXY_TARGET=http://api:8001
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - looma-local
    depends_on:
      - api
    restart: unless-stopped

volumes:
  sqlite_data:

networks:
  looma-local:
    driver: bridge
